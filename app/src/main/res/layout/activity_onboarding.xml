<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    tools:context=".expview.OnboardingActivity">

    <EditText
        android:id="@+id/editTextTextGoalName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="textPersonName"
        android:minHeight="48dp"
        android:hint="Enter a goal name"
        android:text="" />

    <EditText
        android:id="@+id/editTextTimeLimit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="textPersonName"
        android:minHeight="48dp"
        android:hint="Enter the target time limit"
        android:text="" />

    <ExpandableListView
        android:id="@+id/lvCategory"
        android:layout_width="match_parent"
        android:layout_height="381dp"
        android:childDivider="@color/colorBlack"
        android:divider="@null"
        android:dividerHeight="0dp">

    </ExpandableListView>

    <Button
        android:id="@+id/btn"
        android:layout_width="210dp"
        android:layout_height="50dp"
        android:text="Confirm" />

    <Button
        android:id="@+id/btn2"
        android:layout_width="208dp"
        android:layout_height="50dp"
        android:text="Set another goal" />

</LinearLayout>